name: Main
on: [push, pull_request]
jobs:
  CI:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: install modules
        run: npm install

      - name: Run test
        run: npm test
